<% if object.errors().any? %>
<h3>Oops there were some errors. Please try again:</h3>
<ul>
  <% object.errors().full_messages().each() do |message| %>
  <li><%= message %></li>
  <% end %>
</ul>
<% end %>

#this .errors works for active record, you would have to make a method called .errors if doing this way.
